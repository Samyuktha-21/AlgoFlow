{
  "javascript": {
    "language": "javascript",
    "code": "class ListNode {\n  constructor(val) {\n    this.val = val;\n    this.next = null;\n  }\n}\n\nfunction removeNthFromEnd(head, n) {\n  const dummy = new ListNode(0);\n  dummy.next = head;\n  let fast = dummy, slow = dummy;\n  for (let i = 0; i < n; i++) fast = fast.next;\n  while (fast.next) { fast = fast.next; slow = slow.next; }\n  slow.next = slow.next.next;\n  return dummy.next;\n}\n\nfunction build(arr) {\n  const dummy = new ListNode(0);\n  let tail = dummy;\n  for (const v of arr) { tail.next = new ListNode(v); tail = tail.next; }\n  return dummy.next;\n}\nconst res = removeNthFromEnd(build([1, 2, 3, 4, 5]), 2);\nconst out = [];\nfor (let n = res; n; n = n.next) out.push(n.val);\nconsole.log(out.join(' '));",
    "lineKeys": {}
  },
  "java": {
    "language": "java",
    "code": "public class RemoveNthNode {\n    public ListNode removeNthFromEnd(ListNode head, int n) {\n        ListNode dummy = new ListNode(0);\n        dummy.next = head;\n        ListNode fast = dummy, slow = dummy;\n        for (int i = 0; i <= n; i++) fast = fast.next;\n        while (fast != null) { slow = slow.next; fast = fast.next; }\n        slow.next = slow.next.next;\n        return dummy.next;\n    }\n}",
    "lineKeys": {}
  },
  "c": {
    "language": "c",
    "code": "#include <stdlib.h>\ntypedef struct Node{int v;struct Node*next;}Node;\nNode* removeNth(Node*head,int n){Node d;d.next=head;Node*f=&d,*s=&d;for(int i=0;i<=n;i++)f=f->next;while(f){s=s->next;f=f->next;}s->next=s->next->next;return d.next;}",
    "lineKeys": {}
  },
  "cpp": {
    "language": "cpp",
    "code": "struct Node{int v;Node*next;};\nNode* removeNth(Node*h,int n){Node d;d.next=h;Node*f=&d,*s=&d;for(int i=0;i<=n;i++)f=f->next;while(f){s=s->next;f=f->next;}s->next=s->next->next;return d.next;}",
    "lineKeys": {}
  },
  "python": {
    "language": "python",
    "code": "class Node:\n    def __init__(self, val):\n        self.val, self.next = val, None\n\ndef remove_nth_from_end(head, n):\n    dummy = Node(0)\n    dummy.next = head\n    fast = slow = dummy\n    for _ in range(n):\n        fast = fast.next\n    while fast.next:\n        fast = fast.next\n        slow = slow.next\n    slow.next = slow.next.next\n    return dummy.next\n\nhead = Node(1)\ncur = head\nfor v in [2, 3, 4, 5]:\n    cur.next = Node(v)\n    cur = cur.next\nhead = remove_nth_from_end(head, 2)\nout = []\nwhile head:\n    out.append(head.val)\n    head = head.next\nprint(out)",
    "lineKeys": {}
  }
}
