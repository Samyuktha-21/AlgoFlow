{
  "java": {
    "language": "java",
    "code": "public class UnionFind {\n    int[] parent, rank;\n    UnionFind(int n) {\n        parent = new int[n]; rank = new int[n];\n        for (int i = 0; i < n; i++) parent[i] = i;\n    }\n    int find(int x) {\n        if (parent[x] != x) parent[x] = find(parent[x]); // path compression\n        return parent[x];\n    }\n    boolean union(int x, int y) {\n        int rx = find(x), ry = find(y);\n        if (rx == ry) return false; // same set — cycle\n        if (rank[rx] < rank[ry]) parent[rx] = ry;\n        else if (rank[rx] > rank[ry]) parent[ry] = rx;\n        else { parent[ry] = rx; rank[rx]++; }\n        return true;\n    }\n}",
    "lineKeys": {}
  },
  "c": {
    "language": "c",
    "code": "#include <stdio.h>\nint parent[100],rnk[100];\nint find(int x){return parent[x]==x?x:(parent[x]=find(parent[x]));}\nint unionSets(int x,int y){int rx=find(x),ry=find(y);if(rx==ry)return 0;\nif(rnk[rx]<rnk[ry])parent[rx]=ry;else if(rnk[rx]>rnk[ry])parent[ry]=rx;else{parent[ry]=rx;rnk[rx]++;}return 1;}\nvoid init(int n){for(int i=0;i<n;i++){parent[i]=i;rnk[i]=0;}}",
    "lineKeys": {}
  },
  "cpp": {
    "language": "cpp",
    "code": "#include <iostream>\n#include <vector>\nusing namespace std;\nstruct DSU {\n    vector<int> p,r;\n    DSU(int n):p(n),r(n,0){iota(p.begin(),p.end(),0);}\n    int find(int x){return p[x]==x?x:(p[x]=find(p[x]));}\n    bool unite(int a,int b){a=find(a);b=find(b);if(a==b)return false;\n        if(r[a]<r[b])swap(a,b);p[b]=a;if(r[a]==r[b])r[a]++;return true;}\n};",
    "lineKeys": {}
  },
  "python": {
    "language": "python",
    "code": "class UnionFind:\n    def __init__(self, n):\n        self.parent = list(range(n))\n        self.rank = [0] * n\n\n    def find(self, x):\n        while self.parent[x] != x:\n            self.parent[x] = self.parent[self.parent[x]]  # path compression\n            x = self.parent[x]\n        return x\n\n    def union(self, a, b):\n        ra, rb = self.find(a), self.find(b)\n        if ra == rb:\n            return False\n        if self.rank[ra] < self.rank[rb]:\n            ra, rb = rb, ra\n        self.parent[rb] = ra\n        if self.rank[ra] == self.rank[rb]:\n            self.rank[ra] += 1\n        return True\n\nuf = UnionFind(5)\nuf.union(0, 1)\nuf.union(2, 3)\nprint(uf.find(1) == uf.find(0))\nprint(uf.find(1) == uf.find(3))",
    "lineKeys": {}
  }
}
