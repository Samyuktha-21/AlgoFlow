{
  "javascript": {
    "language": "javascript",
    "code": "function kmpSearch(text, pattern) {\n  const lps = new Array(pattern.length).fill(0);\n  let len = 0;\n  for (let i = 1; i < pattern.length;) {\n    if (pattern[i] === pattern[len]) lps[i++] = ++len;\n    else if (len > 0) len = lps[len - 1];\n    else lps[i++] = 0;\n  }\n  const matches = [];\n  let i = 0, j = 0;\n  while (i < text.length) {\n    if (text[i] === pattern[j]) { i++; j++; }\n    if (j === pattern.length) { matches.push(i - j); j = lps[j - 1]; }\n    else if (i < text.length && text[i] !== pattern[j]) {\n      if (j > 0) j = lps[j - 1];\n      else i++;\n    }\n  }\n  return matches;\n}\n\nconsole.log(kmpSearch('ABABDABACDABABCABAB', 'ABABCABAB'));",
    "lineKeys": {}
  },
  "java": {
    "language": "java",
    "code": "public class KMP {\n    int[] buildLPS(String pattern) {\n        int m = pattern.length(), len = 0;\n        int[] lps = new int[m];\n        int i = 1;\n        while (i < m) {\n            if (pattern.charAt(i) == pattern.charAt(len)) { lps[i++] = ++len; }\n            else if (len > 0) { len = lps[len-1]; }\n            else { lps[i++] = 0; }\n        }\n        return lps;\n    }\n    void search(String text, String pattern) {\n        int n = text.length(), m = pattern.length();\n        int[] lps = buildLPS(pattern);\n        int i = 0, j = 0;\n        while (i < n) {\n            if (text.charAt(i) == pattern.charAt(j)) { i++; j++; }\n            if (j == m) { System.out.println(\"Found at \" + (i-j)); j = lps[j-1]; }\n            else if (i < n && text.charAt(i) != pattern.charAt(j)) {\n                if (j > 0) j = lps[j-1]; else i++;\n            }\n        }\n    }\n}",
    "lineKeys": {}
  },
  "c": {
    "language": "c",
    "code": "#include <stdio.h>\n#include <string.h>\nvoid kmp(char*text,char*pat){int n=strlen(text),m=strlen(pat),lps[m],len=0,i=1,j=0;\nlps[0]=0;while(i<m){if(pat[i]==pat[len])lps[i++]=++len;else if(len)len=lps[len-1];else lps[i++]=0;}\ni=0;while(i<n){if(text[i]==pat[j]){i++;j++;}if(j==m){printf(\"Found at %d\\n\",i-j);j=lps[j-1];}else if(i<n&&text[i]!=pat[j]){if(j)j=lps[j-1];else i++;}}}",
    "lineKeys": {}
  },
  "cpp": {
    "language": "cpp",
    "code": "#include <string>\n#include <vector>\n#include <iostream>\nusing namespace std;\nvector<int> kmpSearch(const string&text,const string&pat){int n=text.size(),m=pat.size();\nvector<int>lps(m,0),res;int len=0,i=1;while(i<m){if(pat[i]==pat[len])lps[i++]=++len;else if(len)len=lps[len-1];else lps[i++]=0;}\ni=0;int j=0;while(i<n){if(text[i]==pat[j]){i++;j++;}if(j==m){res.push_back(i-j);j=lps[j-1];}else if(i<n&&text[i]!=pat[j]){if(j)j=lps[j-1];else i++;}}return res;}",
    "lineKeys": {}
  },
  "python": {
    "language": "python",
    "code": "def build_lps(pat):\n    lps, length = [0] * len(pat), 0\n    i = 1\n    while i < len(pat):\n        if pat[i] == pat[length]:\n            length += 1\n            lps[i] = length\n            i += 1\n        elif length:\n            length = lps[length - 1]\n        else:\n            lps[i] = 0\n            i += 1\n    return lps\n\ndef kmp_search(text, pat):\n    lps, hits = build_lps(pat), []\n    i = j = 0\n    while i < len(text):\n        if text[i] == pat[j]:\n            i += 1; j += 1\n            if j == len(pat):\n                hits.append(i - j)\n                j = lps[j - 1]\n        elif j:\n            j = lps[j - 1]\n        else:\n            i += 1\n    return hits\n\nprint(kmp_search(\"ababcababcabc\", \"abc\"))",
    "lineKeys": {}
  }
}
