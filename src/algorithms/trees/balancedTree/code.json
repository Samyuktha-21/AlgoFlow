{
  "javascript": {
    "language": "javascript",
    "code": "class TreeNode {\n  constructor(val) {\n    this.val = val;\n    this.left = null;\n    this.right = null;\n  }\n}\n\nfunction isBalanced(root) {\n  let balanced = true;\n  const height = (node) => {\n    if (!node) return 0;\n    const lh = height(node.left);\n    const rh = height(node.right);\n    if (Math.abs(lh - rh) > 1) balanced = false;\n    return 1 + Math.max(lh, rh);\n  };\n  height(root);\n  return balanced;\n}\n\nconst root = new TreeNode(1);\nroot.left = new TreeNode(2);\nroot.right = new TreeNode(3);\nroot.left.left = new TreeNode(4);\nconsole.log(isBalanced(root));",
    "lineKeys": {}
  },
  "java": {
    "language": "java",
    "code": "public class BalancedTree {\n    public boolean isBalanced(TreeNode root) {\n        return checkHeight(root) != -1;\n    }\n    private int checkHeight(TreeNode node) {\n        if (node == null) return 0;\n        int left = checkHeight(node.left);\n        if (left == -1) return -1;  // left subtree unbalanced\n        int right = checkHeight(node.right);\n        if (right == -1) return -1; // right subtree unbalanced\n        if (Math.abs(left - right) > 1) return -1; // current node unbalanced\n        return 1 + Math.max(left, right);\n    }\n}",
    "lineKeys": {}
  },
  "c": {
    "language": "c",
    "code": "#include <stdio.h>\n#include <stdlib.h>\ntypedef struct Node{int v;struct Node*l,*r;}Node;\nint checkH(Node* n){if(!n)return 0;int l=checkH(n->l);if(l==-1)return -1;\nint r=checkH(n->r);if(r==-1)return -1;if(abs(l-r)>1)return -1;return 1+(l>r?l:r);}\nint isBalanced(Node* root){return checkH(root)!=-1;}",
    "lineKeys": {}
  },
  "cpp": {
    "language": "cpp",
    "code": "#include <iostream>\n#include <cmath>\nusing namespace std;\nstruct Node{int v;Node*l,*r;};\nint checkH(Node*n){if(!n)return 0;int l=checkH(n->l);if(l==-1)return-1;\nint r=checkH(n->r);if(r==-1)return-1;if(abs(l-r)>1)return-1;return 1+max(l,r);}\nbool isBalanced(Node*root){return checkH(root)!=-1;}",
    "lineKeys": {}
  },
  "python": {
    "language": "python",
    "code": "class Node:\n    def __init__(self, val):\n        self.val, self.left, self.right = val, None, None\n\ndef is_balanced(root):\n    def check(node):\n        if not node:\n            return 0\n        left = check(node.left)\n        if left == -1:\n            return -1\n        right = check(node.right)\n        if right == -1 or abs(left - right) > 1:\n            return -1\n        return 1 + max(left, right)\n    return check(root) != -1\n\nroot = Node(1)\nroot.left, root.right = Node(2), Node(3)\nroot.left.left = Node(4)\nprint(is_balanced(root))\nroot.left.left.left = Node(5)\nprint(is_balanced(root))",
    "lineKeys": {}
  }
}
