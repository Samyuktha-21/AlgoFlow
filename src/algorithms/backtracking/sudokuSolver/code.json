{
  "javascript": {
    "language": "javascript",
    "code": "function solveSudoku(board) {\n  const isValid = (row, col, num) => {\n    for (let i = 0; i < 9; i++) {\n      if (board[row][i] === num || board[i][col] === num) return false;\n      const boxRow = 3 * Math.floor(row / 3) + Math.floor(i / 3);\n      const boxCol = 3 * Math.floor(col / 3) + (i % 3);\n      if (board[boxRow][boxCol] === num) return false;\n    }\n    return true;\n  };\n  const solve = () => {\n    for (let row = 0; row < 9; row++) {\n      for (let col = 0; col < 9; col++) {\n        if (board[row][col] === 0) {\n          for (let num = 1; num <= 9; num++) {\n            if (isValid(row, col, num)) {\n              board[row][col] = num;\n              if (solve()) return true;\n              board[row][col] = 0;\n            }\n          }\n          return false;\n        }\n      }\n    }\n    return true;\n  };\n  solve();\n  return board;\n}\n\nconst puzzle = [ [5, 3, 0, 0, 7, 0, 0, 0, 0], [6, 0, 0, 1, 9, 5, 0, 0, 0], [0, 9, 8, 0, 0, 0, 0, 6, 0], [8, 0, 0, 0, 6, 0, 0, 0, 3], [4, 0, 0, 8, 0, 3, 0, 0, 1], [7, 0, 0, 0, 2, 0, 0, 0, 6], [0, 6, 0, 0, 0, 0, 2, 8, 0], [0, 0, 0, 4, 1, 9, 0, 0, 5], [0, 0, 0, 0, 8, 0, 0, 7, 9], ];\nconsole.log(solveSudoku(puzzle)[0].join(''));\n",
    "lineKeys": {}
  },
  "java": {
    "language": "java",
    "code": "public class SudokuSolver {\n    public boolean solve(char[][] board) {\n        for (int r = 0; r < 9; r++) for (int c = 0; c < 9; c++) {\n            if (board[r][c] == '.') {\n                for (char d = '1'; d <= '9'; d++) {\n                    if (isValid(board, r, c, d)) {\n                        board[r][c] = d;\n                        if (solve(board)) return true;\n                        board[r][c] = '.'; // backtrack\n                    }\n                }\n                return false; // no digit works\n            }\n        }\n        return true; // all cells filled\n    }\n    boolean isValid(char[][] b, int r, int c, char d) {\n        for (int i = 0; i < 9; i++) {\n            if (b[r][i] == d || b[i][c] == d) return false;\n            int br = 3*(r/3)+i/3, bc = 3*(c/3)+i%3;\n            if (b[br][bc] == d) return false;\n        }\n        return true;\n    }\n}",
    "lineKeys": {}
  },
  "c": {
    "language": "c",
    "code": "#include <stdio.h>\n#include <stdbool.h>\nchar grid[9][9];\nbool isValid(int r, int c, char d) {\n    for (int i=0; i<9; i++) {\n        if (grid[r][i]==d||grid[i][c]==d)return false;\n        if (grid[3*(r/3)+i/3][3*(c/3)+i%3]==d)return false;\n    }\n    return true;\n}\nbool solve() {\n    for (int r=0; r<9; r++)for (int c=0; c<9; c++)if (grid[r][c]=='.') {\n        for (char d='1'; d<='9'; d++)if (isValid(r, c, d)) {\n            grid[r][c]=d;\n            if (solve())return true;\n            grid[r][c]='.';\n        }\n        return false;\n    }\n    return true;\n}\n",
    "lineKeys": {}
  },
  "cpp": {
    "language": "cpp",
    "code": "#include <vector>\nusing namespace std;\nbool isValid(vector<vector<char>>&b, int r, int c, char d) {\n    for (int i=0; i<9; i++) {\n        if (b[r][i]==d||b[i][c]==d)return false;\n        if (b[3*(r/3)+i/3][3*(c/3)+i%3]==d)return false;\n    }\n    return true;\n}\nbool solve(vector<vector<char>>&b) {\n    for (int r=0; r<9; r++)for (int c=0; c<9; c++)if (b[r][c]=='.') {\n        for (char d='1'; d<='9'; d++)if (isValid(b, r, c, d)) {\n            b[r][c]=d;\n            if (solve(b))return true;\n            b[r][c]='.';\n        }\n        return false;\n    }\n    return true;\n}\n",
    "lineKeys": {}
  },
  "python": {
    "language": "python",
    "code": "def solve_sudoku(board):\n    def valid(r, c, ch):\n        box = 3 * (r // 3), 3 * (c // 3)\n        for i in range(9):\n            if board[r][i] == ch or board[i][c] == ch:\n                return False\n            if board[box[0] + i // 3][box[1] + i % 3] == ch:\n                return False\n        return True\n\n    def backtrack():\n        for r in range(9):\n            for c in range(9):\n                if board[r][c] == 0:\n                    for ch in range(1, 10):\n                        if valid(r, c, ch):\n                            board[r][c] = ch\n                            if backtrack():\n                                return True\n                            board[r][c] = 0\n                    return False\n        return True\n\n    backtrack()\n    return board\n\ngrid = [[5,3,0,0,7,0,0,0,0],[6,0,0,1,9,5,0,0,0],[0,9,8,0,0,0,0,6,0],\n        [8,0,0,0,6,0,0,0,3],[4,0,0,8,0,3,0,0,1],[7,0,0,0,2,0,0,0,6],\n        [0,6,0,0,0,0,2,8,0],[0,0,0,4,1,9,0,0,5],[0,0,0,0,8,0,0,7,9]]\nfor row in solve_sudoku(grid):\n    print(row)",
    "lineKeys": {}
  }
}
