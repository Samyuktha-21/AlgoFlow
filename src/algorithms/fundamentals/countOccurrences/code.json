{
  "java": {
    "language": "java",
    "code": "import java.util.HashMap;\nimport java.util.Map;\npublic class CountOccurrences {\n    public Map<Integer, Integer> count(int[] arr) {\n        Map<Integer, Integer> freq = new HashMap<>();\n        for (int x : arr) freq.merge(x, 1, Integer::sum);\n        return freq;\n    }\n    public static void main(String[] args) {\n        Map<Integer,Integer> freq = new CountOccurrences().count(new int[]{1,2,2,3,3,3,1});\n        freq.forEach((k,v) -> System.out.println(k + \" → \" + v));\n    }\n}",
    "lineKeys": {}
  },
  "c": {
    "language": "c",
    "code": "#include <stdio.h>\nvoid countOccurrences(int a[],int n){int freq[1000]={0};for(int i=0;i<n;i++)freq[a[i]]++;for(int i=0;i<1000;i++)if(freq[i])printf(\"%d->%d\\n\",i,freq[i]);}",
    "lineKeys": {}
  },
  "cpp": {
    "language": "cpp",
    "code": "#include <unordered_map>\n#include <vector>\n#include <iostream>\nusing namespace std;\nauto countOcc(vector<int>&a){unordered_map<int,int>m;for(int x:a)m[x]++;return m;}",
    "lineKeys": {}
  }
}