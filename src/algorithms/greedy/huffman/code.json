{
  "javascript": {
    "language": "javascript",
    "code": "class Node {\n  constructor(char, freq, left = null, right = null) {\n    this.char = char;\n    this.freq = freq;\n    this.left = left;\n    this.right = right;\n  }\n}\n\nfunction huffman(freqs) {\n  let nodes = Object.entries(freqs).map(([char, freq]) => new Node(char, freq));\n  while (nodes.length > 1) {\n    nodes.sort((a, b) => a.freq - b.freq);\n    const left = nodes.shift();\n    const right = nodes.shift();\n    nodes.push(new Node(null, left.freq + right.freq, left, right));\n  }\n  const codes = {};\n  const assign = (node, code) => {\n    if (!node) return;\n    if (node.char !== null) {\n      codes[node.char] = code || '0';\n      return;\n    }\n    assign(node.left, code + '0');\n    assign(node.right, code + '1');\n  };\n  assign(nodes[0], '');\n  return codes;\n}\n\nconsole.log(JSON.stringify(huffman({ a: 5, b: 9, c: 12, d: 13, e: 16, f: 45 })));\n",
    "lineKeys": {}
  },
  "java": {
    "language": "java",
    "code": "import java.util.*;\npublic class Huffman {\n    static class Node implements Comparable<Node> {\n        char ch; int freq; Node left, right;\n        Node(char c, int f) { ch=c; freq=f; }\n        Node(int f, Node l, Node r) { freq=f; left=l; right=r; }\n        public int compareTo(Node o) { return freq - o.freq; }\n    }\n    static void printCodes(Node root, String code) {\n        if (root == null) return;\n        if (root.left == null && root.right == null)\n            System.out.println(root.ch + \": \" + code);\n        printCodes(root.left, code + \"0\");\n        printCodes(root.right, code + \"1\");\n    }\n    public static void buildHuffman(char[] chars, int[] freqs) {\n        PriorityQueue<Node> pq = new PriorityQueue<>();\n        for (int i = 0; i < chars.length; i++) pq.add(new Node(chars[i], freqs[i]));\n        while (pq.size() > 1) {\n            Node l = pq.poll(), r = pq.poll();\n            pq.add(new Node(l.freq + r.freq, l, r));\n        }\n        printCodes(pq.poll(), \"\");\n    }\n    public static void main(String[] args) {\n        char[] chars = {'a','b','c','d','e','f'};\n        int[]  freqs = {45, 13, 12, 16,  9,  5};\n        buildHuffman(chars, freqs);\n    }\n}",
    "lineKeys": {}
  },
  "c": {
    "language": "c",
    "code": "#include <stdio.h>\n#include <stdlib.h>\ntypedef struct Node {\n    char ch;\n    int freq;\n    struct Node *l, *r;\n} Node;\nNode* mk(char c, int f) {\n    Node*n=calloc(1, sizeof(Node));\n    n->ch=c;\n    n->freq=f;\n    return n;\n}\nvoid print(Node* n, char* buf, int d) {\n    if (!n->l && !n->r) {\n        buf[d]=0;\n        printf(\"%c: %s\\n\", n->ch, buf);\n        return;\n    }\n    if (n->l) {\n        buf[d]='0';\n        print(n->l, buf, d+1);\n    }\n    if (n->r) {\n        buf[d]='1';\n        print(n->r, buf, d+1);\n    }\n}\n/* Use a min-heap or sorted array to extract two minimum freq nodes each step */\n",
    "lineKeys": {}
  },
  "cpp": {
    "language": "cpp",
    "code": "#include <bits/stdc++.h>\nusing namespace std;\nstruct Node {\n    char ch;\n    int freq;\n    Node *l, *r;\n};\nvoid printCodes(Node* n, string code=\"\") {\n    if (!n) return;\n    if (!n->l && !n->r) {\n        cout << n->ch << \": \" << code << \"\\n\";\n        return;\n    }\n    printCodes(n->l, code+\"0\");\n    printCodes(n->r, code+\"1\");\n}\nvoid huffman(vector<pair<char, int>>& v) {\n    auto cmp = [](Node* a, Node* b) {\n        return a->freq > b->freq;\n    };\n    priority_queue<Node*, vector<Node*>, decltype(cmp)> pq(cmp);\n    for (auto& [c, f] : v) pq.push(new Node{c, f, nullptr, nullptr});\n    while (pq.size() > 1) {\n        auto l=pq.top();\n        pq.pop();\n        auto r=pq.top();\n        pq.pop();\n        pq.push(new Node{0, l->freq+r->freq, l, r});\n    }\n    printCodes(pq.top());\n}\n",
    "lineKeys": {}
  },
  "python": {
    "language": "python",
    "code": "import heapq\n\ndef huffman_codes(freq):\n    heap = [[f, i, ch, None, None] for i, (ch, f) in enumerate(freq.items())]\n    heapq.heapify(heap)\n    count = len(heap)\n    while len(heap) > 1:\n        lo = heapq.heappop(heap)\n        hi = heapq.heappop(heap)\n        heapq.heappush(heap, [lo[0] + hi[0], count, None, lo, hi])\n        count += 1\n    codes = {}\n    def walk(node, code):\n        if node[2] is not None:\n            codes[node[2]] = code or \"0\"\n            return\n        walk(node[3], code + \"0\")\n        walk(node[4], code + \"1\")\n    walk(heap[0], \"\")\n    return codes\n\nfreq = {'a': 5, 'b': 9, 'c': 12, 'd': 13, 'e': 16, 'f': 45}\nprint(huffman_codes(freq))",
    "lineKeys": {}
  }
}
