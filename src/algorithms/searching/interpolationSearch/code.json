{
  "javascript": {
    "language": "javascript",
    "code": "function interpolationSearch(a, target) {\n  let lo = 0, hi = a.length - 1;\n  while (lo <= hi && target >= a[lo] && target <= a[hi]) {\n    if (lo === hi) return a[lo] === target ? lo : -1;\n    const pos = lo + Math.floor(((target - a[lo]) * (hi - lo)) / (a[hi] - a[lo]));\n    if (a[pos] === target) return pos;\n    if (a[pos] < target) lo = pos + 1;\n    else hi = pos - 1;\n  }\n  return -1;\n}\n\nconsole.log(interpolationSearch([10, 20, 30, 40, 50, 60, 70, 80, 90], 70));",
    "lineKeys": {}
  },
  "java": {
    "language": "java",
    "code": "public class InterpolationSearch {\n    public int search(int[] arr, int target) {\n        int lo = 0, hi = arr.length - 1;\n        while (lo <= hi && target >= arr[lo] && target <= arr[hi]) {\n            if (lo == hi) return arr[lo] == target ? lo : -1;\n            int pos = lo + (int)(((long)(target - arr[lo]) * (hi - lo)) / (arr[hi] - arr[lo]));\n            if (arr[pos] == target) return pos;\n            if (arr[pos] < target) lo = pos + 1;\n            else hi = pos - 1;\n        }\n        return -1;\n    }\n    public static void main(String[] args) {\n        int[] arr = {10,12,13,16,18,19,20,21,22,23,24,33,35,42,47};\n        System.out.println(new InterpolationSearch().search(arr, 18));\n    }\n}",
    "lineKeys": {}
  },
  "c": {
    "language": "c",
    "code": "#include <stdio.h>\nint interpolationSearch(int a[], int n, int t) {\n    int lo=0, hi=n-1;\n    while (lo<=hi&&t>=a[lo]&&t<=a[hi]) {\n        if (lo==hi) return a[lo]==t?lo:-1;\n        int pos=lo+((long)(t-a[lo])*(hi-lo))/(a[hi]-a[lo]);\n        if (a[pos]==t) return pos;\n        if (a[pos]<t) lo=pos+1;\n        else hi=pos-1;\n    }\n    return -1;\n}\nint main() {\n    int a[]= {10, 12, 13, 16, 18, 19, 20, 21, 22, 23};\n    printf(\"%d\\n\", interpolationSearch(a, 10, 18));\n    return 0;\n}\n",
    "lineKeys": {}
  },
  "cpp": {
    "language": "cpp",
    "code": "#include <iostream>\n#include <vector>\nusing namespace std;\nint interpolationSearch(const vector<int>& a, int t) {\n    int lo=0, hi=a.size()-1;\n    while (lo<=hi&&t>=a[lo]&&t<=a[hi]) {\n        if (lo==hi) return a[lo]==t?lo:-1;\n        int pos=lo+((long)(t-a[lo])*(hi-lo))/(a[hi]-a[lo]);\n        if (a[pos]==t) return pos;\n        if (a[pos]<t) lo=pos+1;\n        else hi=pos-1;\n    }\n    return -1;\n}\nint main() {\n    vector<int> a= {10, 12, 13, 16, 18, 19, 20, 21, 22, 23};\n    cout<<interpolationSearch(a, 18)<<endl;\n    return 0;\n}\n",
    "lineKeys": {}
  },
  "python": {
    "language": "python",
    "code": "def interpolation_search(a, target):\n    low, high = 0, len(a) - 1\n    while low <= high and a[low] <= target <= a[high]:\n        if a[low] == a[high]:\n            return low if a[low] == target else -1\n        # estimate position from value distribution\n        pos = low + (target - a[low]) * (high - low) // (a[high] - a[low])\n        if a[pos] == target:\n            return pos\n        elif a[pos] < target:\n            low = pos + 1\n        else:\n            high = pos - 1\n    return -1\n\nprint(interpolation_search([10, 20, 30, 40, 50, 60, 70], 50))",
    "lineKeys": {}
  },
  "lineMap": {
    "c": {
      "2": 2,
      "6": 6,
      "7": 7,
      "8": 8
    },
    "cpp": {
      "2": 4,
      "6": 8,
      "7": 9,
      "8": 10
    },
    "python": {
      "2": 1,
      "6": 7,
      "7": 8,
      "8": 10
    },
    "javascript": {
      "2": 1,
      "6": 5,
      "7": 6,
      "8": 7
    }
  }
}
